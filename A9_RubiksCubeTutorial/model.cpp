#include "model.h"
#include <QBrush>



Model::Model()
{
    red = Faces(Qt::red);
    white = Faces(Qt::white);
    orange = Faces(QColor(255,165,0));
    green = Faces(Qt::green);
    yellow = Faces(Qt::yellow);
    blue =Faces(Qt::blue);
    red = Faces(Qt::red);

}
